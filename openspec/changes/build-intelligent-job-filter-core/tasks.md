# 任务列表 (Tasks): 智能化招聘信息抓取与筛选工具

## 1. 规约与环境自检 (Mandatory Setup)
- [x] 1.1 确认当前分支为 `issue-N` 或 `task/N` (当前仍在 main，请手动建立分支)。
- [x] 1.2 确认 `.gemini/ops_changelog.md` 审计表格已准备好记录动作。
- [ ] 1.3 确认已获取 DeepSeek API Key (请用户手动提供)。

## 2. 基础架构搭建 (Scaffolding)
- [ ] 2.1 初始化 `package.json` 并安装 `axios`, `dotenv`, `fs-extra`。
- [ ] 2.2 建立 `.env` 配置文件 (存储 API Key)。
- [ ] 2.3 设计 `JobFilter` 核心类骨架。

## 3. 历史索引与去重逻辑 (Sentinel)
- [ ] 3.1 实现扫描指定目录并提取 `[公司名]_[职位名]` 指纹的逻辑。
- [ ] 3.2 编写指纹库比对单元测试，确保去重准确性。

## 4. API 集成与 Prompts 工程 (Search)
- [ ] 4.1 构造符合需求（城市、技能、行业双向过滤）的 DeepSeek Prompt。
- [ ] 4.2 实现 API 调用并处理 JSON 结构化解析。
- [ ] 4.3 实现发布时间窗口校验（自动计算当前日期并对比）。

## 5. 存储与交互流实现 (Output & Interaction)
- [ ] 5.1 实现 Markdown 表格生成与追加写入。
- [ ] 5.2 实现交互式参数录入 (求职城市、岗位/技能、薪资、行业偏好等)。
- [ ] 5.3 实现批次（20个）限制及暂停/继续询问的交互逻辑。

## 6. 验证与归档 (Verify & Archive)
- [ ] 6.1 运行集成测试：进行一轮 40 个岗位的抓取（模拟两次循环）。
- [ ] 6.2 检查输出文件格式与命名规范。
- [ ] 6.3 物理归档变更。
